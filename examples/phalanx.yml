#
# General settings
#
host: "0.0.0.0"
bind_port: 2000
grpc_port: 5000
http_port: 8000

# seed_addresses: ["127.0.0.1:2000"]

roles: ["indexer", "searcher"]

index_metastore_uri: "etcd://phalanx/metastore?endpoints=127.0.0.1:2379"

#
# Certification
#
#certificate_file: "./etc/cete-cert.pem"
#key_file: "./etc/cete-key.pem"
#common_name: "localhost"

#
# Cross-Origin Resource Sharing
#
#cors_allowed_methods: ["GET", "PUT", "DELETE", "POST"]
#cors_allowed_origins: ["http://localhost:8080"]
#cors_allowed_headers: ["Content-Type"]

#
# Logging settings
#
log_level: "DEBUG"
log_file: ""
log_max_size: 500
log_max_backups: 3
log_max_age: 30
log_compress: false
